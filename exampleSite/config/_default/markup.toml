# -- Markup --
# split markup into separate file

[goldmark]
  [goldmark.renderer]
    unsafe = true
  [goldmark.renderHooks.image]
    useEmbedded = "fallback"
  [goldmark.renderHooks.link]
    useEmbedded = "fallback"


[highlight]
  noClasses = false

[tableOfContents]
  startLevel = 2
  endLevel = 6
  ordered = false
